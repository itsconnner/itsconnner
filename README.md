# Autoconfig for barroit

### Usage

run `./init` to create the corresponding file symlinks

### Init onedrive

```bash
sudo apt update
sudo apt install rclone
rclone config
# create onedrive remote ...
~/onedrive
```
